<script lang="ts">
  // The standalone Marketplace storefront route (model studio::market, issue
  // #10) — a separate full-screen UX from the editor, reached from the homepage
  // launcher. The store + sign-in gate live in the component.
  import Marketplace from "$lib/components/Marketplace.svelte";
</script>

<Marketplace />
