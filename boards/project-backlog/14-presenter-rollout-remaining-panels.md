---
column: backlog
labels: [extension, webview, tests]
priority: med
agent: opus-docs
live: false
status: `docs` landed — nine of eleven webviews covered. Next is `skills` (presenter first), then the `nav` decision. Parked in backlog between items so the doing column reflects who is actually working.
progress: 82
updatedAt: 2026-07-31T05:40:00.000Z
---
# Roll presenters out to the remaining panels, extending the message contract as they land

The explicit follow-on left by cards 08 and 09. Three panels now have
presenters — marketplace (the pilot), My Mods (#40), and the console (card 08)
— and the declared message contract in `src/core/app/webviewContract.ts` covers
exactly those with typed unions on both sides (console, marketplace and — as of
the first checklist item below — My Mods).

Three webviews remain named in `UNCOVERED_WEBVIEWS`
(`src/core/app/webviewContract.ts`): docs, nav,
skills — `log` came off the list with the second checklist
item below, `publish` with the third, `setup` and `newproject` with the fourth and fifth, and `manifest` with the sixth. The census test
(`test/integration/webview/webviewContract.test.ts`) holds that list to exactly
the `previews/` directory, so every extension shrinks a checked list rather
than an unwritten one.

Order of attack, by decision-logic density rather than alphabetically: the
audit's G5 table (`docs/04-quality/01-testing-audit.md:140-160`) ranks the
panels; `log` and `publish` carried the most logic of what remains. `nav` is a
`WebviewView`, not a panel — it may need its own shape, per card 07's finding
that `navView` hand-rolls teardown for structural reasons.

Per card 09's rule: **a union for a panel whose host half is welded to
`vscode` would leave that half unexecutable — presenter first, contract
second.** One panel per change, each verified the way card 08 was.

## Checklist

- [x] Add My Mods to the contract (presenter already exists)
- [x] `log` — presenter + contract entry
- [x] `publish` — presenter + contract entry
- [x] `setup` — presenter + contract entry
- [x] `newproject` — presenter + contract entry
- [x] `manifest` — presenter + contract entry
- [x] `docs` — presenter + contract entry
- [ ] `skills` — presenter + contract entry
- [ ] Decide what, if anything, `nav` needs
- [ ] Empty `UNCOVERED_WEBVIEWS`, making the census assertion total

## Comments

- **opus-contract** (2026-07-29T08:45:00.000Z): Took the FIRST checklist item only — My Mods into the contract — leaving the other panels alone; one panel per change is the rule card 09 set and this change keeps it. Enumerated both directions from the code before declaring anything, per card 09's warning against inferring from source text: `media/mymods.js:198-249` wires 13 distinct posts (`refresh`, `enable`, `disable`, `uninstall`, `update`, `launch`, `stop`, `openDir`, `openExternal`, `openDocs`, `createShortcut`, `revealBat`, `cleanUninstall`) and its router at `media/mymods.js:252-277` consumes 4 (`init`, `busy`, `progress`, `entrypoint`); `MyModsPresenter.handle` (`src/core/app/myModsPresenter.ts:129-189`) switches on exactly those 13 and posts exactly those 4 (`:232`, `:243`, `:256`, `:276`, `:315`, `:321`, `:328`). **13 / 4, and no mismatch in either direction** — every message one side sends the other handles. Worth noting one route is not obvious from `mymods.js` alone: `openDocs` comes from the shared script-execution notice's Learn-more button (`media/shared.js:181-187`, `data-docs="sandbox"`), which `media/mymods.js:247-249` picks up by `[data-docs]` — exactly the "several dispatch shapes" the audit said a regex contract gets wrong.
- **opus-contract** (2026-07-29T08:45:00.000Z): Declared `MyModsWebviewMessage`/`MyModsHostMessage` at `src/core/app/webviewContract.ts:208-222` and `:224-235`, with the mapped-type key tables at `:237-259` and `MYMODS_PROTOCOL` at `src/core/app/webviewContract.ts:303-309`, following the console/marketplace pattern exactly (`toHost`/`toWebview` are `Object.keys` over the mapped types, so an array cannot drift from its union without a compile error). `silent: []` — unlike the console's `explorerConfig`, all four host pushes render. `mymods` removed from `UNCOVERED_WEBVIEWS` (`src/core/app/webviewContract.ts:327-335`), which is now eight names; the integration census (`test/integration/webview/webviewContract.test.ts:43-46`) picked it up with no edit beyond a stale "two panels" comment, and its scripts/preview check (`:57-67`) confirms `previews/mymods.html` really loads only `media/mymods.js`. Presenter typed to the declaration: `post` is `MyModsHostMessage` (`src/core/app/myModsPresenter.ts:114-119`) and `MyModsInbound` is now an alias of the declared union (`src/core/app/myModsPresenter.ts:51-58`), matching how `ConsoleInbound`/`MarketplaceInbound` are named. Closing the inbound type forced one honest structural change in `handle`: the hoisted `const repo = msg.repo` no longer type-checks against a discriminated union whose members do not all carry a repo, so narrowing is per case (`src/core/app/myModsPresenter.ts:129-189`). Behaviour is identical — the guards, the ordering and the effects are unchanged — and the existing 700-line presenter suite passes untouched apart from one cast where a test deliberately posts an undeclared type (`test/unit/install/myModsPresenter.test.ts:697-703`), the same concession cards 09 made for console and marketplace.
- **opus-contract** (2026-07-29T08:45:00.000Z): Both halves executed, in the layers that can actually execute them. Host half in the unit layer: a My Mods harness (`test/unit/core/webviewContract.test.ts:213-311`), an exhaustive `Record<MyModsInbound["type"], Drive<…>>` drive plan (`:355-369`) asserted key-for-key against `MYMODS_PROTOCOL.toHost`, each of the 13 asserted to be *acted on* (`:561-568`), a negative control proving "was acted on" can come back false (`:570-574`), and a scripted run asserted to produce exactly the 4 declared `toWebview` types (`:577-594`). Webview half in e2e (`tests/webviewContract.spec.ts:244-304`): the real `media/mymods.js` driven in Chromium against `previews/fixtures/mymods.js`, set-equality on what it posted and every push asserted consumed via the harness probe. Two drive notes: the per-mod buttons are spread across *different* mods on purpose (Update and Uninstall latch their row busy, and a latched row's buttons are disabled, so driving them all through one mod would be driving a page a user could not), and `busy`/`progress` have no fixture reply behind them so they are pushed directly — the real host only emits them off a lifecycle action.
- **opus-contract** (2026-07-29T08:45:00.000Z): Falsifiability, three mutations, each reverted. (1) Dropping `revealBat` from `MYMODS_PROTOCOL.toHost`: unit "drives exactly the declared message set" fails (12 keys vs 11) **and** the e2e set-equality fails with `+ "revealBat"` — both ends catch it independently. (2) Emptying `toHost` and `toWebview` to `[]`: **3 unit tests fail** (the non-empty guard plus both My Mods set equalities) and the e2e spec fails — the table cannot be quietly emptied into a green suite. (3) Deleting the `busy` branch from `media/mymods.js`'s router: e2e fails, and after also removing the drive's own `toBeDisabled` expectation it *still* fails, on `busy — Expected: true, Received: false` — the consumption probe catches a dropped handler on its own, so the `changed` flags are not riding on hand-written DOM assertions.
- **opus-contract** (2026-07-29T08:45:00.000Z): Gates, run serially, no coverage-ignore added anywhere. Green: `npm run lint`, `npm run compile`, `npm run typecheck:tests`, `npm run coverage:unit` (**1193 tests, 100%** statements/branches/functions/lines), `npm run coverage:e2e` (**246 tests, 100% across 14 webview scripts**, `media/mymods.js` among them). **`npm run coverage:integration` did NOT pass on this machine, and not because of this change**: 6 tests in `test/integration/adapters/linkerStrategies.test.ts` fail with `EPERM … symlinkSync`, i.e. the process lacks the Windows create-symlink privilege (Developer Mode off). Verified pre-existing by `git stash`ing the whole diff and re-running the same file at `eca03be` — identical 6 failures — and by a bare `fs.symlinkSync` in a temp dir, which also returns `EPERM`. The rest of the layer is green: 49 files / 906 passed with that one file excluded, including the contract census. Flagging rather than papering over it — the integration gate is genuinely unverified here and needs a re-run on a box with the privilege.
- **opus-contract** (2026-07-29T08:50:00.000Z): Closed the doc loop card 09 opened: `docs/04-quality/01-testing-audit.md:385-398` said "two of eleven webviews" and "the other nine" — now three and eight, and it names this card alongside 09 so the next reader can find the increment. Card left in `doing` with `live: false` and an honest status: five checklist items remain and the next one (`log`) needs a presenter written before a union is worth declaring, per card 09's rule.
- **opus-log-presenter** (2026-07-29T21:15:00.000Z): Took the SECOND checklist item only — `log`, presenter first then contract, per card 09's rule. Enumerated both directions from the running code before declaring anything: `media/log.js` posts exactly three (`ready` at `media/log.js:370` from the bottom of the IIFE, `clear` at `:102`, `openSettings` at `:106`) and its router at `media/log.js:338-368` consumes five (`init`, `append`, `reset`, `fileState`, `mod`); the old panel's `onMessage` switched on exactly those three and posted exactly those five. **3 / 5, and no mismatch in either direction.** Two things worth recording because they are not obvious from the file: this page loads `media/shared.js` like every other, but unlike My Mods it has no script-execution notice, so `shared.js` contributes no `openDocs` here and `scripts: ["log.js"]` is the whole webview half; and `openSettings` is reachable ONLY from inside the missing-file pane (`media/log.js:52`, hidden while the file is `ok`), which is why the e2e drive has to push `fileState: "missing"` before it can click it.
- **opus-log-presenter** (2026-07-29T21:15:00.000Z): Extracted `LogPresenter` to `src/core/app/logPresenter.ts:60-152`, following card 08's shape. State is the buffer plus the four things around it that were the panel's own — mod identity, the drop-count watermark, the file state and the file path (`src/core/app/logPresenter.ts:61-66`). Moved: the "any failure means no mod identity" mapping (`:71-80`), the retarget-and-drop-the-old-install rule (`:87-92`), lines→`append`-or-continuation with the silence guard and the drop DELTA (`:95-112`), the two one-liner pushes (`:116-127`), and the handshake replay (`:130-151`). `openSettings` is a `LogEffect` (`:31`) — the same treatment card 08 gave `launchBridge`. Left in the shell on purpose (`src/log/logPanel.ts`): the tailer's lifetime and cadence (cadence is an adapter concern, per card 08 — the shell hands the presenter's `retarget()` result to a new `LogTailer` and wires its three callbacks at `src/log/logPanel.ts:105-116`), the manifest URI + `workspace.fs.readFile` (`:87-93`), and card 07's teardown ordering **unchanged**: `disposed = true` FIRST, then `LogPanel.current = undefined`, then `tailer?.stop()`, inside `disposeWithPanel` at `src/log/logPanel.ts:56-61`, with the `if (this.disposed) return` guard still the first line of `restartTailer` (`:104-107`). The shell went 172 → 130 lines; 11 `vscode.` references remain, 5 of them type positions. One deliberate narrowing: the manifest dep is `parseManifest: (text) => { project?: { name?: string | null } }` (`src/core/app/logPresenter.ts:45-51`) rather than the whole `ManifestPort` — all the log identity needs, and honest about a hand-written TOML reader over a file the user edits.
- **opus-log-presenter** (2026-07-29T21:15:00.000Z): Declared `LogWebviewMessage`/`LogHostMessage` at `src/core/app/webviewContract.ts:262-291` with the mapped-type key tables at `:293-306` and `LOG_PROTOCOL` at `src/core/app/webviewContract.ts:339-345`; `silent: []` — all five host pushes render something. `log` removed from `UNCOVERED_WEBVIEWS` (`src/core/app/webviewContract.ts:369-376`), now seven names, and the integration census (`test/integration/webview/webviewContract.test.ts:43-67`) picked it up with no edit at all, including its check that `previews/log.html` really loads only `media/log.js` beside the always-there `shared.js`. `LogFileState` is declared once, in the contract (`src/core/app/webviewContract.ts:262-266`), and `src/log/tailer.ts:23-25`'s long-standing `FileState` is now an alias of it — the state the tailer reports and the state the webview is told can no longer drift apart.
- **opus-log-presenter** (2026-07-29T21:15:00.000Z): Both halves executed. Host half in unit: a full 25-case presenter suite with no `vscode` double at all (`test/unit/log/logPresenter.test.ts:1-330`), plus the contract's own harness and an exhaustive `Record<LogInbound["type"], LogDrive>` drive plan (`test/unit/core/webviewContract.test.ts:369-383`) asserted key-for-key against `LOG_PROTOCOL.toHost`, each of the three asserted acted on, a negative control, and a scripted run producing exactly the five declared `toWebview` types (`:658-706`). **One honest departure from cards 08/09/14's harness shape, and the reason for it:** `clear` is the first declared message whose entire job is to change host state and answer nothing, so a probe that counts posts and effects scores it as ignored. The log harness therefore measures a `fingerprint()` (`test/unit/core/webviewContract.test.ts:311-356`) — posts + effects + what the presenter WOULD replay if the webview handshook right now, with the reply diverted away from `posted` so reading the state is not itself an interaction. Webview half in e2e (`tests/webviewContract.spec.ts:307-370`): the real `media/log.js` driven in Chromium against `previews/fixtures/log.js`, set-equality on what it posted and every push asserted consumed.
- **opus-log-presenter** (2026-07-29T21:15:00.000Z): **The e2e consumption probe needed a real fix to see `append` at all, and the first version of that fix was wrong in a way the existing suite caught.** `media/log.js:255` batches its append render into `requestAnimationFrame`, so the document is byte-identical the instant the dispatch returns and the harness scored the grid's hundred new rows as "not consumed". First attempt: re-check the document one frame later whenever a push looked unchanged. That turned console's deliberately-`silent` `explorerConfig` green and failed `tests/webviewContract.spec.ts:94` — the window credited a message with whatever some other pending task happened to do. Fixed by making the deferred measurement *attributable*: the harness wraps `requestAnimationFrame` to count requests (`previews/harness.js:28-36`) and only re-measures when THIS dispatch asked for a frame (`previews/harness.js:41-63`), registering its callback after the router's so it runs after it in the same frame. Console, marketplace and My Mods are unaffected — `media/log.js` is the only script in `media/` that calls `requestAnimationFrame` at all.
- **opus-log-presenter** (2026-07-29T21:15:00.000Z): Falsifiability, five mutations, each reverted — and **one of them found a real hole in my own tests, which is now closed**. (1) Dropping `openSettings` from `LOG_TO_HOST_KEYS`: does not even compile (`TS2741` at the mapped type), and with types ignored the unit drive-plan equality fails (3 keys vs 2) **and** the e2e set-equality fails independently. (2) Emptying `LOG_PROTOCOL.toHost`/`toWebview` to `[]`: **3 unit tests fail** (the non-empty guard plus both log set-equalities) and the e2e spec fails — the table cannot be quietly emptied into a green suite. (3) Deleting the `mod` branch from `media/log.js`'s router: e2e fails, and after also removing the drive's own `toBeHidden` expectation it *still* fails on `mod — Expected: true, Received: false`, so the consumption probe catches a dropped handler on its own. (4) Changing the moved drop-count decision from the delta (`this.buffer.droppedCount - this.lastDropped`) to the cumulative total: **all 24 unit tests still passed** — my batches never dropped twice in a row, so delta and total were the same number every time. Added the case that tells them apart (`test/unit/log/logPresenter.test.ts:225-233`); the mutation now fails, and the test says why it matters (the webview ADDS each push to a badge it keeps itself, so a cumulative count double-counts every eviction for the rest of the session). (5) Weakening the silence guard to always post: 1 unit test fails. No behaviour change and no defect found in the panel while moving the logic — the pre-existing 26-case integration suite passed untouched against the new shell before I trimmed it.
- **opus-log-presenter** (2026-07-29T21:15:00.000Z): Integration suite re-layered to the wiring it is now the only witness for: `test/integration/log/logPanel.test.ts` went 26 → 17 cases (panel/singleton/column/CSP document, the manifest read and its no-workspace and unreadable paths, each of the three tailer callbacks reaching the presenter, the settings-change restart and its filter, the handshake receiver, the `openSettings` effect becoming `dcs.setup.open`, and all three teardown cases including the panel closed inside the async manifest window). Gates, run serially, no coverage-ignore added anywhere. Green: `npm run lint`, `npm run compile`, `npm run typecheck:tests`, `npm run coverage:unit` (**1223 tests, 100%** statements/branches/functions/lines), `npm run coverage:e2e` (**247 tests, 100% across 14 webview scripts**, `media/log.js` among them). **`npm run coverage:integration` did not pass on this machine, for the same reason card 14's first agent recorded**: the same 6 tests in `test/integration/adapters/linkerStrategies.test.ts` fail with `EPERM … symlinkSync` (no Windows create-symlink privilege). Verified pre-existing by `git stash`ing the whole diff and re-running that file — identical 6 failures. The rest of the layer is green: 49 files / 898 passed with that one file excluded, the only unmet threshold being `src/adapters/node/linker.ts` (the file those tests cover); `src/log/logPanel.ts` is at 100%. Two environment notes for the next agent on this box: `v8-to-istanbul` is missing from the repo's `node_modules`, so `coverage:e2e` cannot even start until it is installed (`npm install --no-save` inside the worktree, which is what I did — it also pulls a newer Playwright, so `PW_CHROMIUM_PATH` then has to point at an installed Chromium, per card 09).
- **opus-log-presenter** (2026-07-29T21:15:00.000Z): Closed the doc loop the same way card 14's first agent did: `docs/04-quality/01-testing-audit.md:384-400` now reads "four of eleven" and "the other seven", names `log` among the declared unions, and the presenter-extraction bullet records `logPanel` as a 130-line shell over `core/app/logPresenter.ts` (`:401-411`). Card left in `backlog` with `live: false` per the parking convention; four checklist items remain and the next one (`publish`) needs a presenter written before a union is worth declaring.
- **opus-publish** (2026-07-30T03:30:00.000Z): Took the THIRD checklist item only — `publish`, presenter first then contract, per card 09's rule. Enumerated both directions from the running code before declaring anything: `media/publish.js` posts exactly four — `refresh` (twice over: from the bottom of the IIFE at `media/publish.js:215` and from the Re-check button at `:87`), `openExternal` (three separate wirings — the already-shared note's repo link at `:88-95`, and one each rebound inside the `shareDone`/`releaseDone` result panes at `:190-195` and `:206-208`), `share` at `:97-106` and `release` at `:108-135` — and its router at `:155-213` consumes six (`nofolder`, `init`, `log`, `busy`, `shareDone`, `releaseDone`). The old panel's `onMessage` switched on exactly those four and posted exactly those six. **4 / 6, and no mismatch in either direction.** Two things not obvious from the file: this page loads `media/shared.js` like every other but has no script-execution notice, so `shared.js` contributes no `openDocs` and `scripts: ["publish.js"]` is the whole webview half; and `openExternal` is the first message in the contract wired from *three* places in one script, two of them inside host-push handlers rather than in `bind()` — precisely the "several dispatch shapes" the audit said a regex contract gets wrong.
- **opus-publish** (2026-07-30T03:30:00.000Z): **One real finding, journaled rather than fixed, because fixing it is a behaviour change and this card is a move.** In a folderless window the panel is unreachable-by-handshake: the constructor posts `nofolder` synchronously (old `src/publish/publishPanel.ts:47-49`, now `src/core/app/publishPresenter.ts:89-95`) *before* the webview document has loaded, and the webview's own boot handshake — `{type:"refresh"}` at `media/publish.js:215` — is then dropped by the `if (!this.root) return` guard that is the first line of the router (`src/core/app/publishPresenter.ts:142`). So the one message that would re-request the state is the one message the no-folder state refuses to answer, and if the initial push is lost to the load race the user gets an empty Publish panel with nothing on it. Every other covered panel answers its handshake unconditionally. Preserved verbatim (the presenter reproduces both halves exactly, and the pre-existing integration case at `test/integration/publish/publishPanel.test.ts:192-197` still asserts the immediate `nofolder`), and the presenter's `handle` is now the one place a fix would go. Worth its own card.
- **opus-publish** (2026-07-30T03:30:00.000Z): Extracted `PublishPresenter` to `src/core/app/publishPresenter.ts:85-186`, following card 08's shape. Moved: the no-folder-versus-render branch (`:89-95`), the whole `init` payload assembly including the `|| ""` / `|| "0.1.0"` fallbacks (`:98-115`), repo detection over `parseRepoRemote` — a non-GitHub `origin` is "not shared yet", not an error (`:120-123`), **the re-check-at-the-moment-of-action gate and its blocker log line** (`:132-137`), the four-case router with its root guard (`:139-172`), and the busy bracket whose `finally` is the whole point (`:180-186`). `openExternal` is the one `PublishEffect` (`:39`) — the same treatment card 08 gave `launchBridge`. The `e instanceof Error ? … : String(e)` mapping became the existing `core/domain/errorText.ts` rather than a fourth copy. Left in the shell (`src/publish/publishPanel.ts`): the panel, the workspace-root read (`:53`), the fs-and-spawn `preflight`/`readManifest` adapters (`:54-55`), and card 07's teardown **unchanged** — `disposeWithPanel(panel, () => { PublishPanel.current = undefined; })` at `src/publish/publishPanel.ts:50-52`, byte-for-byte the same closure as before, since this panel starts nothing that is not a `Disposable`. The shell went 158 → 90 lines; 6 `vscode.` references remain. Two deliberate narrowings: `root` is a `string | null` VALUE, not a getter (`src/core/app/publishPresenter.ts:56`) — the panel resolves the folder once when it opens and every later decision uses that same root, so a re-read would be a second, different rule; and `msg.opts` stays a cast at `:155` and `:162` rather than becoming a guard, because the union declares what may ARRIVE and it is the service's own argument checks that decide.
- **opus-publish** (2026-07-30T03:30:00.000Z): Declared `PublishWebviewMessage`/`PublishHostMessage` at `src/core/app/webviewContract.ts:310-338` with the mapped-type key tables at `:341-355` and `PUBLISH_PROTOCOL` at `src/core/app/webviewContract.ts:416-422`; `silent: []` — all six host pushes render. `publish` removed from `UNCOVERED_WEBVIEWS` (`src/core/app/webviewContract.ts:442-449`), now six names, and the integration census (`test/integration/webview/webviewContract.test.ts:41-68`) picked it up with no edit at all, including its check that `previews/publish.html` really loads only `media/publish.js` beside the always-there `shared.js`. Two names are declared once here and taken from here by both halves: `PublishBusyScope` (`:314`), because the webview looks its button element up off that exact string pair (`media/publish.js:172`), and `PublishDefaults` (`:317-321`). The message payloads reference the service's own `ShareOpts`/`ShareResult`/`ReleaseOpts`/`ReleaseResult` rather than restating them, so the wire shape and the service signature cannot drift.
- **opus-publish** (2026-07-30T03:30:00.000Z): Both halves executed. Host half in unit: a 19-case presenter suite with no `vscode` double at all (`test/unit/publish/publishPresenter.test.ts:1-314`), plus the contract's own harness (`test/unit/core/webviewContract.test.ts:375-441`) and an exhaustive `Record<PublishInbound["type"], Drive<PublishInbound>>` drive plan (`:523-534`) asserted key-for-key against `PUBLISH_PROTOCOL.toHost`, each of the four asserted acted on, a negative control, and a scripted run producing exactly the six declared `toWebview` types (`:793-836`). The `toWebview` run needs **two** presenters, and that is a fact about the panel rather than a convenience: `nofolder` is a state the panel is CONSTRUCTED in, not one it can reach, so no single instance can produce all six. The plain interactions probe (posts + effects + service calls) is enough here — unlike `log`, publish has no message whose only job is to mutate host state. Webview half in e2e (`tests/webviewContract.spec.ts:373-417`): the real `media/publish.js` driven in Chromium against `previews/fixtures/publish.js` across two loads (the default scenario for the five reachable pushes, `?scenario=nofolder` for the sixth), set-equality on what it posted and every push asserted consumed. No harness change was needed — `media/publish.js` renders inline, so the rAF-probe the log entry above had to add (`previews/harness.js:41-63`) does not come into play.
- **opus-publish** (2026-07-30T03:30:00.000Z): Falsifiability, five mutations, each reverted. (1) Dropping `openExternal` from `PUBLISH_TO_HOST_KEYS`: does not compile (`TS2741` at the mapped type), and with types ignored the unit drive-plan equality fails (4 keys vs 3) **and** the e2e set-equality fails independently with `+ "openExternal"`. (2) Emptying `PUBLISH_PROTOCOL.toHost`/`toWebview` to `[]`: **3 unit tests fail** (the non-empty guard plus both publish set-equalities) and the e2e spec fails — the table cannot be quietly emptied into a green suite. (3) Deleting the `busy` branch from `media/publish.js`'s router: e2e fails on `busy — Expected: true, Received: false`, and this one needed no second step to be conclusive, because the publish drive never asserts on the button's disabled state at all — the consumption probe is the *only* thing watching that handler. (4) Breaking the moved busy bracket, replacing the `finally` with a post on the success path only: 1 unit test fails ("logs a failure and always clears busy") — the stuck-latch regression is caught. (5) Changing the moved version fallback from `||` to `??`, so a manifest with `version = ""` publishes an empty tag box: 1 unit test fails. No behaviour change and no defect found while moving the logic: **the pre-existing 29-case integration suite passed untouched against the new shell** before I re-layered it.
- **opus-publish** (2026-07-30T03:30:00.000Z): Integration suite re-layered to the wiring it is now the only witness for: `test/integration/publish/publishPanel.test.ts` went 29 → 21 cases (the panel/singleton/reveal/teardown, the workspace root reaching the presenter, the real fs-and-manifest-core preflight over mocked `fs` — manifest present/missing/bare-TOML-number, an unbuilt bundle path, a symlinked one, 7z absent — the two mid-flight re-check refusals that only real fs facts can produce, the service calls asserted on the root and opts they were handed, the `openExternal` effect, and the `VsCodeManifest` adapter). Gates, run serially, no coverage-ignore added anywhere. Green: `npm run lint`, `npm run compile`, `npm run typecheck:tests`, `npm run coverage:unit` (**1249 tests, 100%** statements/branches/functions/lines), `npm run coverage:e2e` (**248 tests, 100% across 14 webview scripts**, `media/publish.js` among them). **`npm run coverage:integration` did not pass on this machine, and neither failure is this change**: the same 6 `EPERM … symlinkSync` tests in `test/integration/adapters/linkerStrategies.test.ts` the two agents above recorded (no Windows create-symlink privilege), plus a 7th — `test/integration/docs/bridgeApiDocs.test.ts` reports `docs/03-reference/03-bridge-api-mission.md` drifted from the mission bridge's OpenRPC JSON over card 17's "sim not pumping" wording, i.e. a `npm run docs:bridge` owed by this branch's earlier cards. Verified both pre-existing by `git stash`ing the whole diff and re-running those two files — **identical 7 failures at base**. The rest of the layer is green: 48 files / 888 passed with those two excluded, the only unmet threshold being `src/adapters/node/linker.ts` (the file the EPERM tests cover); `src/publish/publishPanel.ts` is at 100%. Environment notes for the next agent on this box, correcting the entry above: `v8-to-istanbul` is still missing from `node_modules` (`npm install --no-save` in the worktree fixes it), and the Chromium that `PW_CHROMIUM_PATH` must point at is `chromium-1228/chrome-win64/chrome.exe` — `chrome-win/` (no `64`) does not exist and every one of the 248 specs fails in 3ms with "executable doesn't exist" if you use it.
- **opus-setup** (2026-07-30T10:15:00.000Z): Took the FOURTH checklist item only — `setup`, presenter first then contract, per card 09's rule. Enumerated both directions from the running code before declaring anything: `media/setup.js` posts exactly three — `redetect` (the Re-detect button, `media/setup.js:115-117`), `browse` (one wiring, but four buttons, picked up by `[data-browse]` at `:130-134` with the role in the dataset) and `save` (`:143-151`, which trims all four boxes on the way out) — and its router at `:154-180` consumes three (`init`, `browsed`, `saved`). The old panel's `onMessage` switched on exactly those three and posted exactly those three. **3 / 3, and no mismatch in either direction.** Three things worth recording because they are not obvious from the file: this page loads `media/shared.js` like every other but has no script-execution notice, so `shared.js` contributes no `openDocs` and `scripts: ["setup.js"]` is the whole webview half; the candidate rows post NOTHING at all (`:135-142` mutates script-local state and re-renders — picking a detected folder is not a message, which is why the drive has to reach the Browse button for `browse`); and **this is the first covered panel with no handshake in either direction** — `media/setup.js:182` renders an empty form at load and waits, so `init` is pushed unprompted from the constructor and is the only thing that ever fills the screen.
- **opus-setup** (2026-07-30T10:15:00.000Z): **Two real findings, journaled rather than fixed, because both are behaviour changes and this card is a move.** (1) **`browsed.valid` is computed and thrown away.** The host does a real filesystem probe of the role's witness path — `Config` for userdata, `bin\DCS.exe` for an install, the file itself for 7-Zip — and ships the verdict in the message (`src/core/app/setupPresenter.ts:150-166`), but `media/setup.js:167-172` only stores the path and re-renders; the validity line at `:36-46` is derived by looking the path up among the DETECTED candidates, which a hand-browsed path is by definition not in. So a user who browses to the wrong folder gets no warning at all, and the pill they would have got had the same folder been auto-detected is the one thing the host already knows. The pre-existing integration suite asserted `valid` on the wire (old `test/integration/setup/setupPanel.test.ts:143-206`) which is why this survived — the assertion was true and the field was still dead. Preserved verbatim; the fixture now sends `valid` too (`previews/fixtures/setup.js:72-81`) so the preview shows the real payload. (2) **The two sides disagree about what a role-less `browse` means.** The host defaults an absent `which` to userdata — that is what the dialog's label says (`src/core/app/setupPresenter.ts:91-105`, `:150-158`) — while `media/setup.js:167-171` routes a `browsed` with no `which` into the *install* box via its `else`. Reachable only from a stale or crafted post, and the old panel had exactly the same split, so it is preserved: the presenter echoes `which` back as it ARRIVED rather than as it was defaulted (`test/unit/setup/setupPresenter.test.ts:212-227` pins that), because a presenter that "helpfully" filled it in would make the two fallbacks fight instead of merely disagreeing. Both worth a card; a fix belongs in `media/setup.js`, which is the half that is wrong in each.
- **opus-setup** (2026-07-30T10:15:00.000Z): Extracted `SetupPresenter` to `src/core/app/setupPresenter.ts:107-194`, following card 08's shape. Moved: the whole `init` payload including the four `?.trim() ?? ""` fallbacks and the `sevenZipDetected ?? ""` (`:112-133`) — every one of them exists because the webview binds the value straight into `value="…"`, where `undefined` renders as the literal text "undefined"; the per-role browse decision (`:150-166`), which is now a described REQUEST (`SetupBrowseRequest`, `:39-46`) rather than a dialog — file-versus-folder, the confirm label and the `exe` filter are the panel's judgement and the shell only performs it; the per-role validation with its two judgements (`:172-179`) — a role with no witness path is valid by default, and a path the OS refuses to probe is invalid rather than fatal; and the four-write save with its `?? ""` per field (`:186-193`), so clearing a box reaches the settings file. The notification became a `SetupEffect` (`:29`) — the same treatment card 08 gave `launchBridge`. Left in the shell (`src/setup/panel.ts`): the panel, the `getConfiguration` read/write pair, `showOpenDialog`, `fs.existsSync`, and the `USERPROFILE || os.homedir()` read. The shell went 160 → 127 lines; 9 `vscode.` references remain. Card 07's teardown is **unchanged, byte-for-byte** — `disposeWithPanel(panel, () => { SetupPanel.current = undefined; })` at `src/setup/panel.ts:53-55`, the same closure as before, since this panel starts nothing that is not a `Disposable`. Two deliberate narrowings: the `detect` and `archive` deps are now three thunks (`detectSavedGames`, `detectGameInstalls`, `detectedSevenZip`) rather than the `DetectService` class and the `ArchivePort` — the panel only ever needed those three questions, and `ArchivePort` also carries `extract`/`packagePayload`, which are the installer's business and never this panel's (#61's point, taken one step further); and the inline `path.join(home, "DCSStudio", "mods")` became the existing `core/domain/dcsDetect.ts:61-65`'s `defaultDataDir`, which was already the same expression in the same repo.
- **opus-setup** (2026-07-30T10:15:00.000Z): Declared `SetupWebviewMessage`/`SetupHostMessage` at `src/core/app/webviewContract.ts:374-414` with the mapped-type key tables at `:416-426` and `SETUP_PROTOCOL` at `src/core/app/webviewContract.ts:495-501`; `silent: []` — all three host pushes render. `setup` removed from `UNCOVERED_WEBVIEWS` (`src/core/app/webviewContract.ts:522-528`), now five names, and the integration census (`test/integration/webview/webviewContract.test.ts:41-68`) picked it up with no edit at all, including its check that `previews/setup.html` really loads only `media/setup.js` beside the always-there `shared.js`. Two names are taken from the domain rather than restated: `SetupRole` (`core/domain/dcsDetect.ts:106`), which is the same four-value set `roleProbePath` switches on, so the role the webview names and the role the probe rule is chosen by cannot drift; and `DcsCandidate`, so the `init` payload's rows are the detector's own output type. One name is declared new — `SetupPaths` (`:360-371`), the four real setting ids — because `save` writes exactly those under exactly those names while `init` echoes them back under shorter ones, and that mapping deserved one home. **One test-support change was needed and is worth its own note:** the shared `vscode` mock recorded `showSaveDialog`'s options but not `showOpenDialog`'s, so what the panel ASKS FOR was unobservable; `test/integration/support/vscode.ts` now records `openDialogOptions` the same way it has long recorded `saveDialogOptions`, which is what lets the integration layer assert that a 7-Zip browse really asks for a file with an `exe` filter and not a folder.
- **opus-setup** (2026-07-30T10:15:00.000Z): Both halves executed. Host half in unit: a 25-case presenter suite with no `vscode` double at all (`test/unit/setup/setupPresenter.test.ts:1-359`), plus the contract's own harness (`test/unit/core/webviewContract.test.ts:452-503`) and an exhaustive `Record<SetupInbound["type"], Drive<SetupInbound>>` drive plan (`:600-604`) asserted key-for-key against `SETUP_PROTOCOL.toHost`, each of the three asserted acted on, a negative control (`:913-931`), and a scripted run producing exactly the three declared `toWebview` types (`:934-948`). The plain interactions probe (posts + effects + calls) is enough here — unlike `log`, setup has no message whose only job is to mutate host state, and unlike `publish` it needs only ONE presenter for the `toWebview` run, because every state this panel has is reachable from the one it opens in. Webview half in e2e (`tests/webviewContract.spec.ts:419-449`): the real `media/setup.js` driven in Chromium against `previews/fixtures/setup.js` in a single load, set-equality on what it posted and every push asserted consumed. No harness change was needed — `media/setup.js` renders inline, so the rAF probe (`previews/harness.js:41-63`) does not come into play.
- **opus-setup** (2026-07-30T10:15:00.000Z): Falsifiability, five mutations, each reverted. (1) Dropping `browse` from `SETUP_TO_HOST_KEYS`: does not compile (`TS2741` at `src/core/app/webviewContract.ts:416`), and with types ignored the unit drive-plan equality fails (3 keys vs 2) **and** the e2e set-equality fails independently with `+ "browse"`. (2) Emptying `SETUP_PROTOCOL.toHost`/`toWebview` to `[]`: **3 unit tests fail** (the non-empty guard plus both setup set-equalities) and the e2e spec fails — the table cannot be quietly emptied into a green suite. (3) Renaming the `browsed` branch in `media/setup.js`'s router: e2e fails on the drive's own `toHaveValue`, and after also removing that expectation it *still* fails on `browsed — Expected: true, Received: false`, so the consumption probe catches a dropped handler on its own. (4) Weakening the moved cancellation test from `picked === null` to `!picked`, so a picker that answered with the empty string is silently treated as cancelled: 1 unit test fails ("posts a pick of the empty string rather than treating it as a cancellation") — this one is the reason the shell's `browse` returns `string | null` rather than a falsy-or-not, and it was worth pinning because the old panel's `if (picked?.[0])` conflated the two. (5) Dropping the `.trim()` from the moved `sevenZip` fallback: 1 unit test fails. No behaviour change and no defect found while moving the logic beyond the two findings above.
- **opus-setup** (2026-07-30T10:15:00.000Z): Integration suite re-layered to the wiring it is now the only witness for: `test/integration/setup/setupPanel.test.ts` went 17 → 9 cases (`test/integration/setup/setupPanel.test.ts:1-192`) (the CSP document, the singleton reveal, card 07's teardown, the configured settings and detected candidates reaching the presenter, the `USERPROFILE`-then-homedir default data dir, a received message routing to the presenter, the two open-dialog shapes the presenter asks for — folder-with-label and exe-with-filter — over the real `fs.existsSync` probe, a cancelled dialog, and the four global `ConfigurationTarget.Global` writes plus the notification). The `<illegal>`-path throw case moved with the guard it covers into the unit layer. Gates, run serially, no coverage-ignore added anywhere. Green: `npm run lint`, `npm run compile`, `npm run typecheck:tests`, `npm run coverage:unit` (**1280 tests, 100%** statements/branches/functions/lines), `npm run coverage:e2e` (**249 tests, 100% across 14 webview scripts**, `media/setup.js` among them). **`npm run coverage:integration` did not pass on this machine, and the failure is not this change**: the same 6 `EPERM … symlinkSync` tests in `test/integration/adapters/linkerStrategies.test.ts` the three agents above recorded (no Windows create-symlink privilege). Verified pre-existing by running that file at base HEAD (`61ad315`) BEFORE touching anything — identical 6 failures. The 7th failure the entry above recorded (`test/integration/docs/bridgeApiDocs.test.ts`) is **gone**: it passes at base now, so the owed `docs:bridge` regeneration has since landed. The rest of the layer is green: 49 files / 898 passed with that one file excluded, the only unmet threshold being `src/adapters/node/linker.ts` (the file the EPERM tests cover); `src/setup/panel.ts` is at 100%. Environment notes for the next agent on this box, both confirmed again: `v8-to-istanbul` is still missing from `node_modules` (`npm install --no-save v8-to-istanbul` in the worktree), and `PW_CHROMIUM_PATH` must be `chromium-1228/chrome-win64/chrome.exe`.
- **opus-setup** (2026-07-30T10:15:00.000Z): Closed the doc loop the same way the three agents above did: `docs/04-quality/01-testing-audit.md:385-400` now reads "six of eleven" and "the other five", names `setup` among the declared unions, and the presenter-extraction bullet records `setup/panel.ts` as a 127-line shell over `core/app/setupPresenter.ts` (`:402-414`). Card left in `backlog` with `live: false` per the parking convention; three checklist items remain and the next one (`newproject`) needs a presenter written before a union is worth declaring.
- **opus-newproject** (2026-07-30T09:45:00.000Z): Took the `newproject` checklist item only — presenter first then contract, per card 09's rule (split the combined "setup, newproject, manifest, docs, skills" line so the tick means one panel, since `setup` is being worked in a parallel worktree and is not in this branch). Enumerated both directions from the running code before declaring anything: `media/newproject.js` posts exactly two — `browse`, wired from **two** places, the location button and the Browse button, through one shared closure (`media/newproject.js:167-171`), and `create`, wired from the button and from Enter in the name field (`:164-166`, `:172`, posted at `:180-186`) — and its router at `:189-216` consumes four (`init`, `browsed`, `error`, `created`). The old panel's `onMessage` switched on exactly those two and posted exactly those four. **2 / 4, and no mismatch in either direction.** Two things not obvious from the file: it loads `media/shared.js` like every other page and reads `dcsUi.iconPaths` at load, but has no script-execution notice, so `shared.js` contributes no `openDocs` and `scripts: ["newproject.js"]` is the whole webview half; and this is the **only** covered panel whose webview posts nothing at all at load — see the next entry.
- **opus-newproject** (2026-07-30T09:45:00.000Z): **Two real findings, journaled and carded rather than fixed, because fixing either is a behaviour change and this card is a move.** (1) **No boot handshake at all** — new card `24-newproject-has-no-boot-handshake`. `media/newproject.js` renders only on `init`, and `init` is pushed unprompted from the constructor (`src/core/app/newProjectPresenter.ts:110-120`, called at `src/project/newProjectPanel.ts:66`) before the document has necessarily attached its listener. Card 22 fixed publish by answering its boot `refresh` unconditionally; New Project has no message to answer, so a lost push leaves a blank panel with no retry. Preserved verbatim, and `pushInit()` is already idempotent, so the fix is a handshake case away. (2) **`created` can never be seen** — new card `25-newproject-created-message-is-unobservable`. The webview's handler (`media/newproject.js:213-215`) only clears `state.creating` without re-rendering, and in both branches the panel is gone immediately after: in place the `close` effect disposes it, into a new folder the `openFolder` effect reloads the host. Declared honestly rather than dropped — it is the panel's one `silent` entry (`src/core/app/webviewContract.ts:470-478`), which means the e2e half now asserts it stays silent, so if it ever starts rendering the declaration fails until it is corrected.
- **opus-newproject** (2026-07-30T09:45:00.000Z): Extracted `NewProjectPresenter` to `src/core/app/newProjectPresenter.ts:102-176`, following card 08's shape. The pure parts were already out (`core/domain/projectForm.ts` for the form defaults and picker start, `core/domain/scaffoldPlan.ts` for validation and the keep-or-write plan); what moved is the sequencing between them: the `init` payload including the catalogue and the separator the webview joins its preview with (`:110-120`), the picker's cancel-means-post-nothing rule (`:124-131`), **the in-place-versus-new-folder branch re-read at the moment of action** (`:148-151`), the two different endings in order (`:152-164`), and the failure rendered into the form rather than closing it (`:171-174`). Four `NewProjectEffect`s — `close`, `notice`, `authorManifest`, `openFolder` (`:35-45`) — the same treatment card 08 gave `launchBridge`. `errorText` from `core/domain/errorText.ts` rather than a fifth copy of the `instanceof Error` mapping. Left in the shell (`src/project/newProjectPanel.ts`): the panel, the workspace-folder read and its file-scheme test (`:70-73`), the two `globalState` key names (`:16-17`), the folder dialog's options (`:75-84`), the scaffold adapters, and card 07's teardown **unchanged** — `disposeWithPanel(panel, () => { NewProjectPanel.current = undefined; })` at `src/project/newProjectPanel.ts:41-43`, byte-for-byte the same closure, since this panel starts nothing that is not a `Disposable`. The shell went 141 → 115 lines; 11 `vscode.` references remain. One deliberate narrowing and one deliberate difference from publish: the scaffold deps promise only the half of `ScaffoldResult` their branch uses (`{ skipped }` and `{ root }`), and `folder` is a **getter**, not a value (`src/core/app/newProjectPresenter.ts:54-66`) — the opposite of `PublishPresenter.root`, because here the read at `create` time is a guard, not a second rule: the form was rendered with whatever folder was open then, and asking to bootstrap "the open folder" with no folder open must create a new one instead of scaffolding into `undefined`.
- **opus-newproject** (2026-07-30T09:45:00.000Z): Declared `NewProjectWebviewMessage`/`NewProjectHostMessage` at `src/core/app/webviewContract.ts:362-389` with the mapped-type key tables at `:391-401` and `NEWPROJECT_PROTOCOL` at `:470-478`; `silent: ["created"]` — the first non-console panel with anything in that list, for the reason the findings entry gives. `newproject` removed from `UNCOVERED_WEBVIEWS` (`:499`), now five names on one line, and the integration census (`test/integration/webview/webviewContract.test.ts:41-68`) picked it up with no edit at all, including its check that `previews/newproject.html` really loads only `media/newproject.js` beside the always-there `shared.js`. The `init` member is an intersection with the domain's own `InitialForm` and carries `readonly TemplateMeta[]` (`src/core/domain/projectForm.ts:15-22`, `src/core/domain/projectTemplates.ts:12-16`) rather than restating either, so the wire shape cannot drift from the defaults function that fills it.
- **opus-newproject** (2026-07-30T09:45:00.000Z): Both halves executed. Host half in unit: a 19-case presenter suite with no `vscode` double at all (`test/unit/project/newProjectPresenter.test.ts:1-270`), plus the contract's own harness (`test/unit/core/webviewContract.test.ts:445-505`) and an exhaustive `Record<NewProjectInbound["type"], Drive<NewProjectInbound>>` drive plan (`:593-598`) asserted key-for-key against `NEWPROJECT_PROTOCOL.toHost`, both messages asserted acted on, a negative control, and a scripted run producing exactly the four declared `toWebview` types (`:928-949`). Like publish, that run needs **two** presenters, and for the same kind of reason: `error` is not a second message but a different world — a scaffold that refuses — so no single harness can produce it alongside `created`. The plain interactions probe (posts + effects + dep calls) is enough here; unlike `log`, no message's only job is to mutate host state. Webview half in e2e (`tests/webviewContract.spec.ts:419-455`): the real `media/newproject.js` driven in Chromium against `previews/fixtures/newproject.js` in one load, set-equality on what it posted and every push asserted consumed — with `error` driven before `created` deliberately, because `error` is the only reply that clears the Creating… latch and a page that has latched cannot submit again. No harness or fixture change was needed: `media/newproject.js` renders inline (no `requestAnimationFrame`, so the rAF probe at `previews/harness.js:41-63` does not come into play) and the fixture already scripted `browsed`, `error` (the reserved name "taken") and `created` (the reserved name "done").
- **opus-newproject** (2026-07-30T09:45:00.000Z): Falsifiability, four mutations, each reverted — and **one of them found a real hole in my own tests, which is now closed**. (1) Dropping `create` from `NEWPROJECT_TO_HOST_KEYS`: does not compile (`TS2741` at the mapped type), and with types ignored the unit drive-plan equality fails (2 keys vs 1) **and** the e2e set-equality fails independently with `+ "create"`. (2) Emptying `NEWPROJECT_PROTOCOL.toHost`/`toWebview` to `[]`: **3 unit tests fail** (the non-empty guard plus both newproject set-equalities) and the e2e spec fails — the table cannot be quietly emptied into a green suite. (3) Deleting the `browsed` branch from `media/newproject.js`'s router: e2e fails, and after also removing the drive's own `toHaveText` expectation it *still* fails, on `browsed — Expected: true, Received: false` — the consumption probe catches a dropped handler on its own. (4) Swapping the moved order in the new-folder branch, so `setPendingOpen` runs AFTER the `openFolder` effect — the exact regression that would leave a new project opened with no manifest form, because opening the folder reloads the host: **all 19 unit tests still passed.** The suite asserted dep calls and effects in two separate arrays, and two arrays cannot witness the order between them. Added a single `timeline` recording every dep call and every effect in one sequence (`test/unit/project/newProjectPresenter.test.ts:22-45,72-88`) and asserted both endings against it (`:172-181`, `:225-229`); the mutation now fails, and the comment says why the order is the rule rather than a detail.
- **opus-newproject** (2026-07-30T09:45:00.000Z): Integration suite re-layered to the wiring it is now the only witness for: `test/integration/project/newProjectPanel.test.ts` went 18 → 12 cases (the unprompted opening push actually reaching the webview, the workspace-folder read and a `vscode-vfs` workspace counting as no folder, the remembered-location key read BY NAME, the dialog's five options and its `defaultUri`, the scaffold adapters called with the extension URI, both `globalState` keys written before `vscode.openFolder` and that command's own args, the panel surviving a thrown scaffold, the in-place ending as `dcs.manifest.author` with no breadcrumb, the kept-files notice as a real information message, and the singleton/reveal/teardown three). That needed one addition to the shared mock: `state.openDialogOptions` (`test/integration/support/vscode.ts:30-32,116,626-627`), mirroring the long-standing `saveDialogOptions` — `showOpenDialog` recorded its replies but never its arguments, so no test in the repo could witness what a panel actually asked the user to pick. Gates, run serially, no coverage-ignore added anywhere. Green: `npm run lint`, `npm run compile`, `npm run typecheck:tests`, `npm run coverage:unit` (**1274 tests, 100%** statements/branches/functions/lines), `npm run coverage:e2e` (**250 tests, 100% across 14 webview scripts**, `media/newproject.js` among them). **`npm run coverage:integration` did not pass on this box, and not because of this change**: the same 6 `EPERM … symlinkSync` tests in `test/integration/adapters/linkerStrategies.test.ts` the three agents above recorded (no Windows create-symlink privilege). Verified pre-existing by `git stash -u`ing the whole diff and re-running that file — **identical 6 failures**. The 7th failure the publish entry recorded (`bridgeApiDocs`) is **gone** — that doc drift has since been regenerated. The rest of the layer is green: 49 files / 887 passed with that one file excluded, the only unmet threshold being `src/adapters/node/linker.ts` (the file the EPERM tests cover); `src/project/newProjectPanel.ts` is at 100%. Box notes for the next agent, both still true: `v8-to-istanbul` is absent from `node_modules` (`npm install --no-save v8-to-istanbul` in the worktree), and `PW_CHROMIUM_PATH` must point at `chromium-1228/chrome-win64/chrome.exe`. One hazard worth recording because it cost me the contract file mid-run: **do not `git checkout <file>` to revert a mutation** — the work is uncommitted, so it reverts the change too, not just the mutation. Copy the file aside first.
- **opus-newproject** (2026-07-30T09:45:00.000Z): Closed the doc loop the same way the three agents above did: `docs/04-quality/01-testing-audit.md:385-400` now reads "six of eleven" and "the other five", names `newproject` among the declared unions, and the presenter-extraction bullet records `newProjectPanel` as a 115-line shell over `core/app/newProjectPresenter.ts` (`:401-413`). Card parked back in `backlog` with `live: false` per the convention; three checklist items remain and the next panel (`manifest`) needs a presenter written before a union is worth declaring.
- **opus-manifest** (2026-07-30T18:30:00.000Z): Took the `manifest` checklist item only — presenter first then contract, per card 09's rule (split the combined "manifest, docs, skills" line so the tick means one panel). Enumerated both directions from the running code before declaring anything: `media/manifest.js` posts exactly **one** — `edit`, from a single debounced emitter (`media/manifest.js:28-34`) that every field, every add and every remove funnels through (`media/manifest.js:292-348`) — and its router at `media/manifest.js:366-377` consumes **two**, `external` and `roots`. The old panel's `onMessage` switched on exactly that one and posted exactly those two. **1 / 2, and no mismatch in either direction.** Three things worth recording because they are not obvious from the file: this is the **only covered panel with no message-borne opening state at all** — `media/manifest.js:9` reads `window.__BOOTSTRAP__` synchronously at load, so its `init` crosses inside the DOCUMENT the host renders, which is why the `toWebview` union has no `init` in it and why this panel cannot lose a handshake to the load race the way publish (card 22) and New Project (card 23) can; the `edit` post is debounced 200ms and carries the WHOLE file, so the host's identical-text refusal is the common path rather than an edge case; and `scripts` is **two** files (`manifest-core.js`, `manifest.js`) — the only non-console protocol with more than one — because the parse/emit/resolve core is shared with the extension host and is loaded beside the form rather than dispatching anything itself (`src/core/app/webviewContract.ts:618-631`).
- **opus-manifest** (2026-07-30T18:30:00.000Z): **This panel is structurally unlike the seven before it, and the difference decided the presenter's shape.** Card 07 recorded it: the form is keyed BY DOCUMENT in a `Map` (`src/manifest/formPanel.ts:20`, `src/manifest/formPanel.ts:43`) rather than a single `current` slot, because a user may have two `dcs-studio.toml` files open and each gets its own form. So there is **one presenter per PANEL INSTANCE**, constructed in the constructor (`src/manifest/formPanel.ts:57-68`) and dying with it — not one per session as every previous presenter is. That is not stylistic: `lastWritten`, the echo watermark that stops the form's own write being pushed back at it, is ONE document's fact (`src/core/app/manifestPresenter.ts:93`), and a presenter shared between two forms would let form A's write suppress form B's external push — the exact clobbering bug the watermark exists to prevent, moved one level up. `test/unit/manifest/manifestPresenter.test.ts:103-114` pins that with two harnesses and says why. Card 07's teardown is preserved **byte-for-byte**: `disposeWithPanel(panel, () => { ManifestFormPanel.panels.delete(this.document.uri.toString()); })` at `src/manifest/formPanel.ts:54-56`, the same closure and the same map delete, with the presenter deliberately knowing nothing about the map.
- **opus-manifest** (2026-07-30T18:30:00.000Z): Extracted `ManifestPresenter` to `src/core/app/manifestPresenter.ts:85-166`, following card 08's shape. Moved: the **echo rule** itself, which is the whole game for a two-way-bound panel (`src/core/app/manifestPresenter.ts:133-136`); the bootstrap payload, which is a value the presenter COMPUTES rather than a message it posts (`:106-112`); the two refusals on an inbound `edit` — not a string, or identical to the document — (`:161-165`); and the roots pair with its `?? ""` (`:121-126`), which is a decision, not a coercion: the form draws `{GameInstall}` as unresolvable-on-this-machine off exactly that emptiness, and `undefined` would render as the literal text "undefined" in the resolved line. **No effects at all** — this is the first covered panel with no `Effect` type, because everything it does to the world it does through the document. Left in the shell (`src/manifest/formPanel.ts`): the panel, the map, the three `vscode.workspace` listeners with their per-document uri filters (`src/manifest/formPanel.ts:70-83`), and the `WorkspaceEdit` (`:61-66`). The shell went 118 → 104 lines; 14 `vscode.` references remain. One deliberate narrowing: the roots dep is two thunks (`savedGames`, `gameInstall`) rather than the whole `InstallRootsPort` (`src/core/app/manifestPresenter.ts:68-71`) — the form has no business with `dataDir`, which is where mods are unpacked, and the two it does need are the installer's own so the resolved-destination preview cannot promise a folder nothing would use.
- **opus-manifest** (2026-07-30T18:30:00.000Z): **Two real findings, journaled and carded rather than fixed, because fixing either is a behaviour change and this card is a move.** (1) **The debounced edit survives an external change and overwrites it** — new card `26-manifest-form-debounced-edit-survives-an-external-change`. `media/manifest.js:369-372` re-seeds the model on `external` but never clears the pending 200ms `editTimer` (`media/manifest.js:28-34`), so a keystroke landing within the debounce window of a revert/undo/`git checkout` is discarded AND the timer still fires, posting an `edit` built from the new model that rewrites the just-changed file in the form's canonical emit and marks it dirty. The host's identical-text refusal only saves it when the emit happens to be byte-identical. (2) **The echo watermark is never cleared, so a redo is swallowed** — new card `27-manifest-echo-watermark-swallows-a-redo`. The rule is text equality against the last text the form wrote (`src/core/app/manifestPresenter.ts:133-136`), so undo-then-redo puts the document back to that text and the `external` push is suppressed: the form renders `T0` while the document holds `T1`, and the next form edit quietly undoes the redo. Both preserved verbatim — the presenter reproduces the old rule exactly, and `test/unit/manifest/manifestPresenter.test.ts:86-101` pins both halves of it — and `onDocumentChanged` is now the one place a fix would go.
- **opus-manifest** (2026-07-30T18:30:00.000Z): Declared `ManifestWebviewMessage`/`ManifestHostMessage` at `src/core/app/webviewContract.ts:515-522` with the mapped-type key tables at `src/core/app/webviewContract.ts:524-531` and `MANIFEST_PROTOCOL` at `src/core/app/webviewContract.ts:618-631`; `silent: []` — both pushes re-render the whole form, since there is nothing this form keeps that it does not draw. Two names are declared beside the messages rather than restated at each end: `ManifestRoots` (`src/core/app/webviewContract.ts:485-493`), whose `gameInstall: string` documents the `""`-not-absent rule the webview's two different warnings depend on, and **`ManifestBootstrap` (`src/core/app/webviewContract.ts:500-506`) — a type that is deliberately NOT a message**, declared here because it is this panel's `init` and the only reason it is not one. `manifest` removed from `UNCOVERED_WEBVIEWS` (`src/core/app/webviewContract.ts:653`), now three names, and the integration census (`test/integration/webview/webviewContract.test.ts:41-68`) picked it up with no edit at all — including its scripts check, which is the one that had something to say here: it confirms `previews/manifest.html` really loads `manifest-core.js` and `manifest.js` in that order beside the always-there `shared.js`. Also repaired a garbled paragraph in the file's header (`src/core/app/webviewContract.ts:47-56`), where two parallel worktrees had merged their coverage sentences into each other.
- **opus-manifest** (2026-07-30T18:30:00.000Z): Both halves executed. Host half in unit: a 14-case presenter suite with no `vscode` double at all (`test/unit/manifest/manifestPresenter.test.ts:1-190`), plus the contract's own harness (`test/unit/core/webviewContract.test.ts:573-618`) and an exhaustive `Record<ManifestInbound["type"], Drive<ManifestInbound>>` drive plan (`test/unit/core/webviewContract.test.ts:727-732`) asserted key-for-key against `MANIFEST_PROTOCOL.toHost`, the one message asserted acted on, a negative control, and a scripted run producing exactly the two declared `toWebview` types (`test/unit/core/webviewContract.test.ts:1150-1166`). The plain interactions probe (posts + writes) is enough — unlike `log`, no message's only job is to mutate host state, since `edit`'s whole point is the write. **One presenter is enough for the `toWebview` run, and for a reason worth recording:** unlike publish and New Project, this panel has no `init` to produce, so both declared pushes are reachable from the state it opens in. The harness's `write` lands in the harness's document, as the real `WorkspaceEdit` does — a write that went nowhere would make every echo test pass for the wrong reason. Webview half in e2e (`tests/webviewContract.spec.ts:493-535`): the real `media/manifest.js` driven in Chromium against `previews/fixtures/manifest.js` in one load, set-equality on what it posted and every push asserted consumed. `edit` needed `expectSent` rather than a straight read, because it arrives 200ms after the keystroke. No harness or fixture change was needed — `media/manifest.js` renders inline, so the rAF probe (`previews/harness.js:41-63`) does not come into play.
- **opus-manifest** (2026-07-30T18:30:00.000Z): Falsifiability, six mutations, each reverted (copied aside first — never `git checkout`, per the hazard the entry above records). (1) Dropping `edit` from `MANIFEST_TO_HOST_KEYS`: does not compile — `TS2741: Property 'edit' is missing in type '{}'` at `src/core/app/webviewContract.ts:524`. (2) Hard-coding `MANIFEST_PROTOCOL.toHost`/`toWebview` to `[]` past the types: **3 unit tests fail** (the non-empty guard plus both manifest set-equalities) and the e2e set-equality fails independently — the table cannot be quietly emptied into a green suite. (3) Deleting the `roots` branch from `media/manifest.js`'s router: e2e fails on the drive's own resolved-dest assertion, and after also removing that expectation it *still* fails, on `roots — Expected: true, Received: false` — the consumption probe catches a dropped handler on its own. (4) Removing the echo guard from `onDocumentChanged` so every change is pushed back: 1 unit test fails ("does not echo the form's own edit back into the form") — the clobber-the-user's-caret regression is caught. (5) Arming the watermark BEFORE the identical-text refusal rather than after: 1 unit test fails ("does not arm the watermark on an edit it refused to write") — subtle, and worth pinning, because that ordering would make a real external edit back to the refused text vanish silently, i.e. it would manufacture card 27's bug on a path that does not have it today. (6) **The map-keyed lifecycle**, mutated on purpose even though the presenter does not touch it: replacing card 07's `panels.delete(...)` teardown with a no-op fails **14 of the 15** integration cases in `test/integration/manifest/formPanel.test.ts` — a form left in the map is revealed instead of re-opened, so every later case in the file gets the previous one's panel. Still heavily load-bearing, and still covered.
- **opus-manifest** (2026-07-30T18:30:00.000Z): Integration suite re-layered to the wiring it is now the only witness for: `test/integration/manifest/formPanel.test.ts` went 20 → 15 cases (the panel opened beside and titled after the file, the bootstrap reaching the webview through the real `installRoots` adapter over a mocked disk — configured paths, the `USERPROFILE` default, the `DCS.openbeta` fallback, the homedir fallback — the one-form-per-document map both ways, each of the three listeners reaching the presenter WITH its per-document filter, the `WorkspaceEdit` itself, and card 07's teardown freeing the map entry). The five that left were the echo rule and the `edit` guards, which are the presenter's and now run with no `vscode` at all. Gates, run serially, no coverage-ignore added anywhere. Green: `npm run lint`, `npm run compile`, `npm run typecheck:tests`, `npm run coverage:unit` (**1323 tests, 100%** statements/branches/functions/lines), `npm run coverage:e2e` (**252 tests, 100% across 14 webview scripts**, `media/manifest.js` among them). **`npm run coverage:integration` did not pass on this box, and not because of this change**: the same 6 `EPERM … symlinkSync` tests in `test/integration/adapters/linkerStrategies.test.ts` the four agents above recorded (no Windows create-symlink privilege). Verified pre-existing by running that file at base HEAD (`d367632`) BEFORE touching anything — identical 6 failures. The rest of the layer is green: 49 files / 873 passed with that one file excluded, the only unmet threshold being `src/adapters/node/linker.ts` (the file the EPERM tests cover); `src/manifest/formPanel.ts` is at 100%. Box notes for the next agent, both confirmed again: `v8-to-istanbul` is absent from `node_modules` (`npm install --no-save v8-to-istanbul` in the worktree), and `PW_CHROMIUM_PATH` must be `chromium-1228/chrome-win64/chrome.exe`.
- **opus-manifest** (2026-07-30T18:30:00.000Z): Closed the doc loop the same way the four agents above did: `docs/04-quality/01-testing-audit.md:384-402` now reads "eight of eleven", names `manifest` among the declared unions and names the other three explicitly (`docs`, `nav`, `skills`), and the presenter-extraction bullet records `manifest/formPanel` as a 104-line shell over `core/app/manifestPresenter.ts` (`docs/04-quality/01-testing-audit.md:403-419`) — noting that it is the first NON-singleton to get one, and that per-document keying is why it takes a presenter per panel instance. Card parked back in `backlog` with `live: false` per the convention; three checklist items remain and the next panel (`docs`) needs a presenter written before a union is worth declaring.
- **opus-docs** (2026-07-31T05:40:00.000Z): Took the `docs` checklist item only — presenter first then contract, per card 09's rule (split the combined "docs, skills" line so the tick means one panel; skills and the `nav` decision are untouched). Enumerated both directions from the running code before declaring anything: `media/docs.js` posts exactly **two** — `run`, from the `[data-command]` branch of its ONE delegated click handler (`media/docs.js:83-87`), and `openExternal`, from the `a[href]` branch of the same handler with an `/^https?:/` test on the href (`media/docs.js:88-92`) — and its router at `media/docs.js:95-98` consumes exactly **one**, `goto`. The old panel's `onMessage` switched on exactly those two and posted exactly that one. **2 / 1, and no mismatch in either direction.** Three things worth recording because they are not obvious from the file: the whole webview half is ONE delegated listener on `#app` with three branches in priority order (`[data-page]` first, which is internal navigation and posts nothing at all, then `[data-command]`, then an external `a[href]`) — the "several dispatch shapes" a regex contract gets wrong, in a single handler; `scripts` is **two** files (`docs-content.js`, `docs.js`) and neither dispatches on its own behalf — `docs-content.js` is the manual itself, read synchronously as `window.__DOCS__`, so a docs panel with no content posts nothing; and like the manifest form this panel has **no boot handshake and no opening message** — `media/docs.js:15` reads `window.__INITIAL_PAGE__` at the top of its IIFE, so the deep link crosses inside the DOCUMENT and `goto` exists only for the panel that is ALREADY open, which is exactly what card 07's note about `show` navigating to a page turns out to mean.
- **opus-docs** (2026-07-31T05:40:00.000Z): **One real finding, journaled and carded rather than fixed, because fixing it is a behaviour change and this card is a move** — new card `28-docs-goto-unknown-page-loses-the-readers-place`. The two halves of the deep-link rule disagree about an UNKNOWN page id: the document path VALIDATES it (`media/docs.js:14-19` — an id not in `pages` is ignored and the reader gets their persisted page), while the message path does not (`media/docs.js:97` hands it straight to `render`, whose `pages.find(...) || pages[0]` at `media/docs.js:52` silently jumps to Overview and then persists Overview at `media/docs.js:55`). So `dcs.docs.open("renamed-page")` does nothing visible on a closed panel and throws an open reader back to the front of the manual mid-read. Reachable from a stale `data-docs` attribute or a renamed page id; the fix belongs in `media/docs.js`, which is the half that is wrong, and the host cannot make the call because the page ids live in `media/docs-content.js`, which only the webview loads. Preserved verbatim — `DocsPresenter.navigate` reproduces the old `if (page)` rule unchanged. **A second, smaller fact worth recording without a card:** `openExternal` is declared and implemented on both halves but is unreachable from the SHIPPED manual — `media/docs-content.js` contains not one `href="https…"` in any page body, so the real content never takes that branch. `tests/docs.spec.ts:53-64` had papered over it by injecting an anchor into the document with `page.evaluate`; the contract drive instead adds a `?docs=links` scenario to `previews/fixtures/docs.js:42-59` whose page body carries a real link, so what is measured is `media/docs.js` rendering content rather than the test building DOM.
- **opus-docs** (2026-07-31T05:40:00.000Z): Extracted `DocsPresenter` to `src/core/app/docsPresenter.ts:52-98`, following card 08's shape. Moved: the **deep-link rule**, which is this panel's one genuine rule and lives in two halves that must agree — `bootstrap()` (`src/core/app/docsPresenter.ts:70-72`) for the panel being opened and `navigate()` (`src/core/app/docsPresenter.ts:84-87`) for the panel being revealed, the latter carrying the decision that matters (`if (!page) return`: "open the documentation" with nothing named must reveal the panel where the reader left it rather than yank them to a page); and the two payload guards on an inbound message (`src/core/app/docsPresenter.ts:89-97`). Two `DocsEffect`s — `runCommand`, `openExternal` (`src/core/app/docsPresenter.ts:36-40`) — the same treatment card 08 gave `launchBridge`. The `?? ""` in `bootstrap` is a decision, not a coercion, and is pinned as one (`test/unit/docs/docsPresenter.test.ts:39-48`): the shell injects the value through `JSON.stringify` into an inline script, and `JSON.stringify(undefined)` is not a string — it renders the literal token `undefined` into the document, where the webview then looks for a page called "undefined". Left in the shell (`src/docs/docsPanel.ts`): the panel, the singleton slot, the rendered document with its inline script and CSP, `vscode.commands.executeCommand` and the `openExternal` helper. Card 07's teardown is preserved **byte-for-byte** — `disposeWithPanel(panel, () => { DocsPanel.current = undefined; })` at `src/docs/docsPanel.ts:40-42`, the same closure as before, since this panel starts nothing that is not a `Disposable`. **Honest note on size: this is the one shell that got LONGER, 67 -> 75 lines**, with 6 `vscode.` references left. The docs panel holds no state whatsoever — content, TOC and navigation are all the webview's — so what moved out is small and what replaced it is the effect dispatch. The gain is not line count but that every decision the panel makes now runs with no `vscode` at all; the shell that is left contains none.
- **opus-docs** (2026-07-31T05:40:00.000Z): Declared `DocsWebviewMessage`/`DocsHostMessage` at `src/core/app/webviewContract.ts:553-567` with the mapped-type key tables at `src/core/app/webviewContract.ts:569-576` and `DOCS_PROTOCOL` at `src/core/app/webviewContract.ts:677-688`; `silent: []` — `goto` re-renders the page pane and re-marks the TOC's active row. One name is declared beside the messages and is deliberately NOT a message: `DocsBootstrap` (`src/core/app/webviewContract.ts:535-551`), the second of these after `ManifestBootstrap` and for the same reason — it is this panel's `init`, and the only reason it is not one is that it crosses inside the document, which is also why the `toWebview` union has no `init` in it. `docs` removed from `UNCOVERED_WEBVIEWS` (`src/core/app/webviewContract.ts:712`), now **two** names — `nav`, `skills` — and the integration census (`test/integration/webview/webviewContract.test.ts:41-68`) picked it up with no edit at all, including its scripts check, which confirms `previews/docs.html` really loads `docs-content.js` and `docs.js` in that order beside the always-there `shared.js`.
- **opus-docs** (2026-07-31T05:40:00.000Z): Both halves executed. Host half in unit: a 10-case presenter suite with no `vscode` double at all (`test/unit/docs/docsPresenter.test.ts:1-126`), plus the contract's own harness (`test/unit/core/webviewContract.test.ts:626-660`) and an exhaustive `Record<DocsInbound["type"], Drive<DocsInbound>>` drive plan (`test/unit/core/webviewContract.test.ts:766-771`) asserted key-for-key against `DOCS_PROTOCOL.toHost`, both messages asserted acted on, a negative control, and a scripted run producing exactly the one declared `toWebview` type (`test/unit/core/webviewContract.test.ts:1231-1242`). The plain interactions probe (posts + effects) is enough — unlike `log`, no message's only job is to mutate host state, because this presenter has no state to mutate. One presenter and one call are enough for the `toWebview` run, for the manifest's reason: no `init` to produce. Webview half in e2e (`tests/webviewContract.spec.ts:540-579`), **two loads, and the second one is the finding above made executable**: session 1 drives the REAL shipped manual for `run` (a content-owned `.cmd-btn`, which the preview loads unmodified) and `goto`; session 2 loads `?docs=links` for `openExternal`, because no page in the shipped manual has an outbound link. No harness change was needed — `media/docs.js` renders inline, so the rAF probe (`previews/harness.js:41-63`) does not come into play.
- **opus-docs** (2026-07-31T05:40:00.000Z): Falsifiability, six mutations, each reverted (copied aside first — never `git checkout`, per the hazard card 14's `newproject` entry records). (1) Dropping `openExternal` from `DOCS_TO_HOST_KEYS`: does not compile — `TS2741: Property 'openExternal' is missing` at `src/core/app/webviewContract.ts:569`. (2) Hard-coding `DOCS_PROTOCOL.toHost` to `["run"]` past the types: the unit drive-plan equality fails (2 keys vs 1) **and** the e2e set-equality fails independently with `+ "openExternal"`. (3) Hard-coding `toHost`/`toWebview` to `[]`: **3 unit tests fail** (the non-empty guard plus both docs set-equalities) and the e2e spec fails — the table cannot be quietly emptied into a green suite. (4) Deleting the `goto` branch from `media/docs.js`'s router: e2e fails on the drive's own page-title assertion, and after also removing that expectation it *still* fails, on `goto — Expected: true, Received: false` — the consumption probe catches a dropped handler on its own. (5) Removing the `if (!page) return` guard from `navigate`, so revealing the docs always navigates: 1 unit test fails ("leaves the reader where they were when no page was named") — the yank-the-reader-to-Overview regression is caught, and it is the same class of bug as card 28's, which is why it was worth pinning. (6) Changing `page ?? ""` to `page` in `bootstrap`: **both layers fail independently** — 1 unit test ("carries the empty string, not undefined") and 1 integration test ("seeds an empty initial page when none is given", which is the one that watches the rendered document). And, as the manifest agent did, card 07's teardown was mutated on purpose even though the presenter does not touch it: replacing the `DocsPanel.current = undefined` closure with a no-op fails "clears the singleton on dispose so the next show re-opens". No behaviour change and no defect found while moving the logic beyond the finding above.
- **opus-docs** (2026-07-31T05:40:00.000Z): Integration suite re-layered to the wiring it is now the only witness for: the `DocsPanel` block in `test/integration/nav/navAndDocs.test.ts:248-303` went 8 -> 6 cases (the panel titled and seeded with the requested page, the empty seed, the reveal-and-navigate singleton, each of the two effects reaching `executeCommand` and `openExternal`, and card 07's teardown). The two that left were the presenter's own — revealing without navigating, and the missing-payload guards — and they now run with no `vscode` at all. Gates, run serially, no coverage-ignore added anywhere. Green: `npm run lint`, `npm run compile`, `npm run typecheck:tests`, `npm run coverage:unit` (**1338 tests, 100%** statements/branches/functions/lines), `npm run coverage:e2e` (**253 tests, 100% across 14 webview scripts**, `media/docs.js` and `media/docs-content.js` among them). **`npm run coverage:integration` did not pass on this box, and not because of this change**: the same 6 `EPERM … symlinkSync` tests in `test/integration/adapters/linkerStrategies.test.ts` the five agents above recorded (no Windows create-symlink privilege). Verified pre-existing by running that file at base HEAD (`fdeee71`) BEFORE touching anything — identical 6 failures. The rest of the layer is green: 49 files / 872 passed with that one file excluded, the only unmet threshold being `src/adapters/node/linker.ts` (the file the EPERM tests cover); `src/docs/docsPanel.ts` is at 100%. Box notes for the next agent, both confirmed again: `v8-to-istanbul` is absent from `node_modules` (`npm install --no-save v8-to-istanbul` in the worktree), and `PW_CHROMIUM_PATH` must be `chromium-1228/chrome-win64/chrome.exe`.
- **opus-docs** (2026-07-31T05:40:00.000Z): Closed the doc loop the same way the five agents above did: `docs/04-quality/01-testing-audit.md:384-401` now reads "nine of eleven", names `docs` among the declared unions and names the remaining two explicitly (`nav`, `skills`), and the presenter-extraction bullet records `docs/docsPanel` as a 75-line shell over `core/app/docsPresenter.ts` (`docs/04-quality/01-testing-audit.md:403-425`) — saying out loud that it is the one shell that grew, and why that is still the right trade. Card parked back in `backlog` with `live: false` per the convention; two checklist items of work remain and the next panel (`skills`) needs a presenter written before a union is worth declaring, after which `nav` is a decision rather than a repetition — it is a `WebviewView`, not a panel.
