---
column: review
labels: [extension, webview]
priority: med
agent: claude
live: false
updatedAt: 2026-07-29T05:53:00.000Z
---
# Extract the panel singleton scaffold

The remaining half of the DRY finding at `src/webview/html.ts:10-16`, scored 0.6
and tracked as #51.

The document boilerplate is shared — `renderWebviewHtml` and `mediaUri` live in
`src/webview/html.ts` — but the singleton scaffold around it is not. Nine panels
each hand-write the same `static current`, `static show`, `panel`, `disposables`,
`onDidDispose -> dispose`, `dispose() { current = undefined; … }` block. The
comment records that they have **already diverged in teardown detail**, and each
divergence needs its own test.

The asset-list half of #51 is closed
(`test/integration/webview/previewAssets.test.ts`), and the capabilities half
lands with PR #67 (card 01) as `webviewCapabilities()`.

PR #67's own writeup warns against a base class, per #51's terms: each panel's
reveal does something different. Whatever shape this takes has to leave that
free.

## Checklist

- [x] Enumerate the nine panels and diff their teardown blocks
- [x] Pick a shape that shares lifecycle without constraining `reveal`
- [x] Keep every panel at 100% under the integration gate
- [x] Remove the TODO at `src/webview/html.ts:10`

## Comments

- **claude** (2026-07-29T05:53:00.000Z): Picked this up off the back of card 01 (#67 merged `src/webview/panel.ts`). First job was the diff the card asks for, before touching anything. Ten webview panels share the scaffold, not nine — the nine with a `static current` slot (`src/skills/skillsPanel.ts:12`, `src/docs/docsPanel.ts:12`, `src/install/myModsPanel.ts:26`, `src/log/logPanel.ts:19`, `src/publish/publishPanel.ts:14`, `src/marketplace/panel.ts:23`, `src/project/newProjectPanel.ts:19`, `src/setup/panel.ts:16`, `src/bridge/consolePanel.ts:24`) plus `src/manifest/formPanel.ts:13`, which is the same lifecycle keyed by document in a `Map` instead of a single slot. `src/nav/navView.ts:15` is a `WebviewView` with its own teardown and is deliberately out of scope; `src/mission/missionPanel.ts` is not a panel at all despite the name (plain command functions). Teardown diff, before any change — six were byte-identical (`current = undefined` → `panel.dispose()` → drain the bag): skills, docs, publish, marketplace, newProject, setup. Four diverged: `src/bridge/consolePanel.ts:269` also `clearInterval(this.pollTimer)`; `src/log/logPanel.ts:155` also sets `this.disposed = true` FIRST and then `this.tailer?.stop()`; `src/install/myModsPanel.ts:143` also resets `this.launcher.setOnChange(() => {})`; `src/manifest/formPanel.ts:98` deletes its map entry rather than clearing a singleton. Every one of the four is deliberate and every one already had its own test, so all four were preserved verbatim rather than unified — see the next entry for the shape that let me do that.
- **claude** (2026-07-29T05:53:00.000Z): Shape chosen: a closure-returning helper, `disposeWithPanel(panel, teardown)` at `src/webview/panel.ts:106-121`, which owns the disposables bag and the `onDidDispose` wiring and hands the bag back. Each panel does `this.disposables = disposeWithPanel(panel, () => { … })` and deletes its own `dispose()`. Justification for NOT a base class, per #51 and #67's writeup: the constrained thing would be `static show`, whose *only* varying part is the reveal branch — `src/docs/docsPanel.ts:21` navigates to a page, `src/skills/skillsPanel.ts:21` and `src/install/myModsPanel.ts:44` re-query, `src/manifest/formPanel.ts:29` reveals Beside with `preserveFocus`, the other five just raise the tab. An inherited `show` would need a hook per panel and would own the `static current` field that `src/extension.ts:232` and nine test files assign directly. I also considered a `revealOrOpen(current, reveal, open)` helper for the static half and rejected it: three closures per call site to remove four lines, and it reads worse. So `show`/`reveal` is left completely alone, and what is shared is only the closing sequence — the part where drift means a leak (a listener still attached to a live signal, or a `current` pointing at a closed panel so it can never re-open). The per-panel differences stay as a one-line closure at the top of each constructor, which makes them *more* visible than they were buried at the bottom of nine near-identical `dispose()` methods.
- **claude** (2026-07-29T05:53:00.000Z): Applied at `src/webview/panel.ts:106`, `src/skills/skillsPanel.ts:34-36`, `src/docs/docsPanel.ts:34-36`, `src/install/myModsPanel.ts:88-91`, `src/log/logPanel.ts:53-60`, `src/publish/publishPanel.ts:43-45`, `src/marketplace/panel.ts:51-53`, `src/project/newProjectPanel.ts:39-41`, `src/setup/panel.ts:43-45`, `src/bridge/consolePanel.ts:50-56`, `src/manifest/formPanel.ts:47-51`. Order inside the helper is fixed and asserted: `teardown()` → `panel.dispose()` → drain the bag last-registered-first. That is exactly the order all ten already used, including `src/log/logPanel.ts`'s `disposed = true` landing before its slot clear, so nothing changed behaviourally. `panel.dispose()` is kept even though it is a no-op on the normal path (we are inside the panel's own dispose event, and the API is idempotent) — it was in all ten copies and it is what keeps the sequence true if teardown is ever reached another way. Stale TODO removed from `src/webview/html.ts:10-16` and replaced with a four-line pointer at `src/webview/html.ts:10-13` that keeps the still-accurate asset-list note (`test/integration/webview/previewAssets.test.ts`).
- **claude** (2026-07-29T05:53:00.000Z): Tests. Added `describe("disposeWithPanel")` at `test/integration/webview/panel.test.ts:60-141` — five cases covering teardown-on-close, the bag draining last-registered-first, teardown running *before* the drain, no double-teardown when the helper closes the panel from inside the panel's own dispose event, and the bag collecting what the `onDid…(listener, thisArg, disposables)` signature pushes into it. The four divergences needed no new tests: each already had one and all four still pass unchanged — `test/integration/bridge/consolePanel.test.ts:534-549` (timer + subscriptions), `test/integration/log/logPanel.test.ts:347-364` (tailer stopped, and no orphan tailer for a panel closed inside the async manifest read), `test/integration/install/myModsPanel.test.ts:168-177` (launcher stops refreshing a closed panel), `test/integration/manifest/formPanel.test.ts:239` (map entry released so the form re-opens). Gates, run serially per ARCHITECTURE.md — `npm run lint` exit 0, `npm run compile` exit 0, `npm run typecheck:tests` exit 0, `npm run coverage:unit` exit 0 (1111 tests, 100%/100%/100%/100%), `npm run coverage:integration` exit 0 (954 tests, 100%/100%/100%/100%). No coverage-ignore comments added; none were needed. Integration statement count fell 2265 → 2222, which is the duplication actually leaving the codebase rather than moving. e2e not run: nothing under `media/` was touched.
- **claude** (2026-07-29T05:53:00.000Z): Mutation evidence, because a refactor that only passes the existing suite proves nothing. Breaking the shared helper: dropping the drain loop at `src/webview/panel.ts:117` fails 4 tests in 2 files; dropping the `teardown()` call at `src/webview/panel.ts:115` fails 39 tests in 12 files; swapping the drain to run *before* teardown fails exactly the one test written for that ordering. Breaking each preserved divergence individually, one at a time: no `clearInterval` in the console teardown → 1 failure; no `tailer?.stop()` in the log teardown → 1 failure; no `disposed = true` in the log teardown → 1 failure (the "closed before the manifest read finished" case); no `launcher.setOnChange` reset in My Mods → 1 failure; no map delete in the manifest form → 22 failures. Every divergence is individually load-bearing and individually guarded. All mutations reverted; the final gate run above is on the clean tree.
- **claude** (2026-07-29T05:53:00.000Z): Two things found in the diff that are NOT bugs, recorded so nobody "fixes" them later. (1) `src/log/logPanel.ts:30` is the only panel carrying a `disposed` flag, and the asymmetry is deliberate rather than an oversight in the other nine: its consequence is unique. Async work queued in the log panel's constructor *starts a file poller* (`restartTailer`), so a panel closed inside that window would leave something polling `dcs.log` for the rest of the session. The comparable queued work elsewhere (`postSkills`, `presenter.refresh`, an in-flight console `poll`) only ends in a `postMessage` to a dead webview, which the API answers `false` to. Copying the flag to the other nine would be nine unreachable guards and nine coverage holes. (2) `this.panel.dispose()` inside every old `dispose()` was already unreachable-as-written — `dispose()` was only ever wired to `onDidDispose`, so the panel was always disposed by the time it ran. I kept it rather than dropping it, because it is what makes the helper's contract ("the panel is closed and everything it held is released") hold regardless of what triggered teardown, and it costs one covered statement in one place instead of ten. Neither is a defect; I found no divergence that was.
- **claude** (2026-07-29T05:53:00.000Z): Deliberately left undone. The `static show` bodies are untouched — that is the point of the shape, not an omission, and `static current` stays a plain public writable field because `src/extension.ts:232` reads it and nine test files assign it. `src/nav/navView.ts:53-60` still hand-rolls its own teardown: it is a `WebviewView`, its lifecycle is per-`resolveWebviewView` rather than per-instance, and it holds named single subscriptions instead of a bag, so `disposeWithPanel` does not fit it — #67 already gave it the one thing it genuinely shared (`webviewCapabilities`). Card is in review; a human moves it to done.
