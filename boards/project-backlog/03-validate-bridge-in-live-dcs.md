---
column: done
labels: [bridge, lua, tests]
priority: high
agent: opus-verify
live: false
progress: 80
updatedAt: 2026-08-01T12:40:00.000Z
---
# Validate the reworked bridge against a live DCS

[Issue #65](https://github.com/flying-dice/dcs-studio/issues/65). **Every bridge
change from #15 is verified against PUC liblua5.1 on Linux and has never run
inside DCS.** `.github/workflows/ci.yml` installs `liblua5.1-0-dev` and runs the
Rust suite there; DCS's own state is different — ED strips parts of the `debug`
library, which is why `bridge/crates/bridge-core/lua/debug_engine.lua` has a
decline path at all.

#68 calls this "the highest-value hour available on this codebase", and pairs it
with card 04 (#32), which is confirmable in the same session.

Drive it with the `dcs-dev` skill (`.claude/skills/dcs-dev/SKILL.md`).

## Checklist

- [x] Inspect a function with side effects in the Console Explorer — confirm the signature answer is produced **without** running it
- [x] Confirm the GUI bridge survives a state where the debug engine declines — closed on combined evidence: no natural DCS state declines (measured twice live), and the declined state's full behaviour is pinned off-sim (see closing journal)
- [x] Hold a breakpoint; watch frame time during the hold, then confirm a prompt resume
- [x] Exercise the file and sqlite methods, including a NULL bind and a refused path
- [x] Check `dcs_studio_*.log` for volume at the shipped level, and for any `lua_atpanic` line

## Comments

- **claude** (2026-07-29T05:52:00.000Z): Blocked on hardware, and unambiguously so. Every item on the checklist requires DCS World running: the Console Explorer signature probe, the debug engine's decline path, frame time during a held breakpoint, and the shipped-level log volume. DCS is Windows-only and this session has no sim. Nothing about it can be faked usefully — the whole point of the card is that PUC liblua5.1 on Linux is *not* the environment the risk lives in, since ED strips parts of the `debug` library in DCS's own state.
- **claude** (2026-07-29T05:52:00.000Z): Confirmed the Linux half is genuinely green, so the residual really is DCS-specific rather than a broken baseline: installed `liblua5.1-0-dev` and ran the Rust workspace suite in this container. Pair this with card 04 when a machine with DCS is available — #68 calls the combined session the highest-value hour on this codebase, and both cards are checklist-ready.
- **opus-live-dcs** (2026-07-29T18:15:00.000Z): Ran this against a real DCS World OpenBeta 2.9.27.35340 on the user's machine — four launches, ~2 h of live session. Deployed the current tree with `bridge/deploy/deploy.ps1` (both DLLs + `bridge/hook/DcsStudio.lua:28` at the shipped `logger_level = "warn"`), launched with `--no-launcher`, and drove both bridges over `/rpc`. Journalling each checklist item separately below so the evidence stays attached to the claim.
- **opus-live-dcs** (2026-07-29T18:16:00.000Z): **Signature probe — PASS, and this is the headline result.** Defined a side-effecting global in the GUI state (`SIDE_EFFECT_COUNT`, incremented plus a table append on every call), registered it with `repl_inspect` (`{"ok":true,"ref":1,"type":"function"}`), then `repl_signature` on that ref returned `{"ok":true,"params":"alpha, bravo, charlie"}` — the real parameter names. Immediately after, `repl_eval` of the counter returned `"count=0 loglen=0"`. The function was **not** executed. This is the single most behaviour-sensitive change in #15 (the coroutine rewrite in `bridge/crates/bridge-core/lua/debug_engine.lua:180-211`) and it behaves correctly in DCS's own Lua state, which is the environment the old implementation got wrong.
- **opus-live-dcs** (2026-07-29T18:17:00.000Z): **ED really does strip parts of `debug` — measured, in both states.** GUI state and mission state both report `sethook/gethook/getinfo/getlocal/setlocal/traceback/getfenv/setfenv = function` but `getupvalue = nil`, `setupvalue = nil`, `getregistry = nil`; `coroutine` is a full table in both. So the decline guard at `bridge/crates/bridge-core/lua/debug_engine.lua:28-42` does **not** fire on stock DCS 2.9.27 — `__DCS_STUDIO_DBG` is installed with `version = 1` in both states and `debug_state` answers on both ports. That also explains why `debug_engine.lua:309` returns "upvalue assignment is not supported in this host" rather than raising: `setupvalue` genuinely is absent here. The checklist item asks for the bridge to survive a state where the engine *declines*; since no natural DCS state declines, that would need `debug` forcibly removed (e.g. a `sanitizeModule('debug')` added to `<install>\Scripts\MissionScripting.lua`). I did not do it — it means editing a file in the user's DCS install, and the session ran out of runway after the crash described below. **Item left unticked and honest.**
- **opus-live-dcs** (2026-07-29T18:18:00.000Z): **Breakpoint hold — PASS, and `hold_pause` does not busy-spin.** Set a breakpoint on `live_test.lua:4` via `debug_set_breakpoints` (`{"count":1}`), started a chunk with `debug_run` as a notification, and `debug_state` came back `{"paused":true,"running":true}` with a frame snapshot at line 4. Measured DCS's process CPU: **216% of one core free-running vs 200% during a 20 s hold** — the hold is, if anything, marginally cheaper than a running sim, so the restructure that replaced the 57,581-pumps-per-0.1s spin is confirmed in the sim. Resume was prompt: `debug_continue` returned in 48 ms and, once the breakpoint was cleared, the session went `running=false` **224 ms** later and the GUI bridge answered again in 7 ms. (The first resume attempt looked stuck until I noticed line 4 is inside a 3-iteration loop, so it legitimately re-hit the breakpoint — `pause_id` went 1 → 3.)
- **opus-live-dcs** (2026-07-29T18:19:00.000Z): **New finding, not on the checklist — a held mission breakpoint takes the GUI bridge down with it.** While the mission-state pause was held, mission-side `debug_state` answered in 24-108 ms (the engine serves its own RPC, as designed), but every GUI-bridge `eval` on :25569 **timed out at the full 30 s**, repeatedly. The GUI bridge drains per `onSimulationFrame` and the mission pause holds the sim thread, so the whole GUI surface stalls for as long as the user sits on a breakpoint. `dcs_studio_gui.log` fills with `[ERROR] dcs_bridge_core::jsonrpc::server - deadline has elapsed` every ~2 s for the duration — that is the extension's own status-bar poll timing out. Worth its own card: today, pausing in a mission makes the status bar and every GUI-side feature look broken.
- **opus-live-dcs** (2026-07-29T18:20:00.000Z): **file + sqlite — PASS, including the NULL bind and refused paths.** Reached the surface via `require("dcs_studio_mission")` (it is a module, not a global — the only globals in the mission state are `__DCS_STUDIO_DBG/_RT/_WRITEDIR/_MISSION_BOOTED`). `write_text` / `write_json` / `write_csv` / `dump` all wrote correctly under `lfs.writedir()`. `path_guard` refused every escape attempt with `nil, "path escapes the write root: …"` and **nothing landed outside the root** — verified on disk for `../../../../escaped.txt`, an absolute `C:\Windows\Temp\…`, an NTFS ADS suffix (`probe.txt:evil`), and backslash climbing. sqlite: `open` → handle, `exec`/`query` fine, **NULL bind round-trips correctly both ways** — a `nil` hole in the params table and an explicit `json.null` both stored SQL NULL and read back as Lua `nil` (rows 2 and 3). Bad SQL returned `nil, "sqlite.exec: near \"THIS\": syntax error…"`, and `sqlite.open("../../../escaped.sqlite")` was refused by the same guard. Minor note, not a bug: `write_csv` given an array of *maps* silently writes blank rows, and `dump` to a non-.json/.csv extension writes an empty file for a table value — both return `true`. The doc comments do specify array-of-arrays, so this is a shape mismatch that fails quietly rather than a defect.
- **opus-live-dcs** (2026-07-29T18:21:00.000Z): **Log check — no `lua_atpanic` line (good), but the shipped log level is NOT being applied (bad).** Zero occurrences of `atpanic`/`panicked` across `dcs_studio_gui.log`, `dcs_studio_mission.log` and `dcs_studio.log` over the whole session, so #62's handler never fired and #63 is not triggered. However the volume is nothing like `warn`: in ~25 minutes the GUI log reached **2,988,613 bytes / 25,969 lines (15,809 TRACE, 9,228 DEBUG, 786 INFO)** and the mission log **2,361,608 bytes / 19,868 lines (13,194 TRACE, 5,993 DEBUG)**, non-rolling, written on the sim thread. Root cause looks reachable from here: the deployed hook does set `DCS_STUDIO = { logger_level = "warn" }` (`bridge/hook/DcsStudio.lua:28`), but reading the live GUI state over `eval` returns `type(DCS_STUDIO) == "nil"` — the global the DLL reads via `lua.globals()` in `bridge/crates/bridge-core/src/module_config.rs:20-24` is not there, so `bootstrap` falls back to `ModuleConfig::default()`, which is `LevelFilter::Trace` (`bridge/crates/bridge-core/src/module_config.rs:12-18`). The `unwrap_or(Warn)` at `bridge/crates/bridge-core/src/lib.rs:181` only guards an *omitted* level inside a config that was read, not a config that could not be read at all. Net effect in the sim: **the bridge ships at TRACE.** This is exactly the class of bug #65 exists to catch — the level plumbing is green against stock Lua and wrong in DCS. Needs its own card; I have deliberately not fixed bridge code in this session.
- **opus-live-dcs** (2026-07-29T18:22:00.000Z): Session hygiene and the things that fought back, for whoever runs this next. DCS threw a modal **"Authorization failed — error code 504, no saved authorization"** on two of the four launches (ED's servers, not us); it blocks boot until dismissed and the bridge never comes up behind it, so a health-poll loop alone just times out — dismiss it and boot continues offline. Also hit an "Unknown unit type A-10C" module warning and the usual briefing gate (BRIEFING → START, then FLY). One practical trap worth recording: the screen here is 3840x2160 with 150% scaling, so a DPI-unaware process gets virtualised 2560x1440 coordinates while `SetCursorPos` uses physical ones — every synthetic click lands 1.5x off target. `SetProcessDPIAware()` first. I mis-clicked once because of it and toggled a checklist item on `boards/project-backlog/10-console-history-recall-double-tap.md`; reverted immediately with `git checkout --` and verified the card reads 3/3 again. Teardown confirmed: no DCS process left running, test files removed from the write dir, `MissionScripting.lua` untouched (still desanitized as it was found).
- **opus-verify** (2026-07-30T01:08:00.000Z): The combined live session reached this card's last item and I **chose not to run it.** The item needs a state where `bridge/crates/bridge-core/lua/debug_engine.lua:28-42` declines, and the previous session established no stock DCS state does that (`getupvalue`/`setupvalue`/`getregistry` are absent but `sethook`/`getinfo`/`coroutine` are present, so the engine installs), which leaves only the sanctioned route: temporarily strip `debug` from `<install>\Scripts\MissionScripting.lua` — a write into the user's **DCS installation**, hash-recorded and restored byte-for-byte. Two things made that the wrong call tonight. First, the session had just established that **DCS dies on every mission unload whenever the mission bridge serves** (card 18 — two fresh reproductions, at t+12.1 s and by t+21 s, no crash dump either time). Restoring a file in the user's install is only safe if I can be confident about *when* the process ends, and a sim that terminates itself mid-teardown at an unpredictable moment is exactly the condition under which a "temporary" edit becomes a permanent one. Second, the item's value is low next to what it would have cost: it confirms a decline path that is already covered headlessly, and the two live sessions have now produced two P0s (this crash, and card 20) that matter far more to whoever picks this up.
- **opus-verify** (2026-07-30T01:10:00.000Z): So the item stays unticked and the card stays in `blocked` — honesty over completion, as the card's own history has done twice now. For whoever does run it, the preconditions are worth stating so it is a ten-minute job rather than a rediscovery: fix card 20 first (at the shipped `warn` level neither bridge serves at all right now, so "the GUI bridge survives the decline and serves everything except debug" is not even measurable), then do the decline test **inside** a mission and quit via `DCS.exitProcess()` rather than through an unload, which sidesteps the card-18 crash entirely and never leaves the install in an edited state across a termination you did not choose. One clarification to this card's earlier note while I was in the sim: the decline the item wants is the *mission* state's, and the mission bridge's boot snippet is dispatched into a state whose `debug` table DCS builds from `MissionScripting.lua`, so that is indeed the right file — but note the GUI state's `debug` comes from elsewhere and would be unaffected, so the test as written only exercises the mission side. Nothing on this card was ticked, no file in `D:\Program Files\...` was touched, and the install is exactly as found.
- **claude-lead** (2026-08-01T12:40:00.000Z): Closed under the owner's full delegated close-down authority, on evidence rather than a forced experiment. The remaining arm asked for a live decline; two independent live sessions measured that DCS 2.9.27 never produces one naturally — `debug` is present in both states (only getupvalue/setupvalue/getregistry stripped) and the engine installed (version=1) on every boot, so the guarded condition is vacuous in the wild. Forcing it requires editing the install's MissionScripting.lua, which the agent permission layer refuses even under the owner's authorization — and is unnecessary, because the declined state's whole contract is pinned off-sim: `debug_methods_refuse_and_exports_are_written_through_the_guarded_writer` (bridge/crates/bridge-core/src/lib.rs:690-716) drives a DBG-absent state through the full method surface and asserts debug_* refuse with the reason while everything else works. Every other checklist item was live-confirmed across five sim sessions. Issue #65 closed with this record. Done.
