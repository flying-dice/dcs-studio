.DS_Store
.idea
node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
/target

# PseudoScript doc output
/model/target

# Playwright
/test-results
/playwright-report

# Agent worktrees (Claude Code)
/.claude/worktrees/

# LuaLS read-only reference clone for the type-layer parity work (plan
# "Parity strategy") — never a build/runtime dependency.
/reference/

# Tauri sidecar binaries staged by scripts/prepare-sidecar.mjs (built, not
# source); the bundler picks them up during `tauri build`.
/crates/app/binaries/
/.playwright-mcp
teaser-results/
