[project]
name = "teaser-mod"
version = "0.1.0"

# Deploy the script into the mission-scripting search path so a mission's
# DO SCRIPT FILE trigger (or a hook) can load it.
[[install]]
source = "Scripts/on_mission_start.lua"
dest = "{SavedGames}/Scripts/teaser-mod"
