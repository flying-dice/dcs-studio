# static/ — resolution and profile diagnostics

Deferred until the model crate lands (plan Phase 2/3). Each `name.lua` will
pair with `name.diagnostics`: one diagnostic code per line, order-independent;
an empty file means well-formed.
