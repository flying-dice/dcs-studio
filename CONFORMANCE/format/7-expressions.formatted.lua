local sum = a + b * c - d / e % f
local pw = 2 ^ 10
local neg = - -x
local len = #list
local inv = not flag
local cat = "n=" .. n .. "!"
local cmp = a <= b and b >= c or a ~= c
local idx = t[1 + 2].field:method(arg)
local paren = (a + b) * (c - d)
local chain = obj.a.b.c[key]
require("module")
f({ x = 1 })
obj:m("s")
